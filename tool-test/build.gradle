plugins {
  id 'build.jar'
}

description = "Toolbox Test Dependencies Management"

java {
  registerFeature('optionalSupport') {
    usingSourceSet(sourceSets.main)
  }
}

dependencies {
  api(platform(project(":tool-dependencies")))

  api("xyz.srclab.common:boat-test")
}